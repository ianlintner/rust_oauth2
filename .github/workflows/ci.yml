name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
    tags:
      - "*.*.*"
      - "v*.*.*"
      - "*.*.*-*"
      - "v*.*.*-*"
  pull_request:
    branches: [main, develop]

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  RUST_BACKTRACE: 1
  OAUTH2_JWT_SECRET: "test-jwt-secret-for-ci-only-do-not-use-in-production-32chars"
  # Always define this so workflow expressions referencing env.IMAGE_VERSION are valid.
  # On tag builds, it's overwritten by the "Compute release version" step.
  IMAGE_VERSION: "0.0.0-dev"

jobs:
  ci:
    name: Rust (fmt, clippy, test)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Job summary (init)
        run: |
          {
            echo "## Rust (fmt, clippy, test)"
            echo "Progress updates will appear below as steps complete."
            echo
            echo "- ✅ Checkout (1/7)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Rust toolchain + cache
        uses: ./.github/actions/rust-setup
        with:
          toolchain: stable
          components: rustfmt, clippy
          cache-key: ci
          install-system-deps: "true"

      - name: Job summary
        run: echo "- ✅ Rust toolchain + cache (2/7)" >> "$GITHUB_STEP_SUMMARY"

      - name: Check Formatting
        run: cargo fmt -- --check

      - name: Job summary
        run: echo "- ✅ cargo fmt (3/7)" >> "$GITHUB_STEP_SUMMARY"

      - name: Clippy Linting
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Job summary
        run: echo "- ✅ cargo clippy (4/7)" >> "$GITHUB_STEP_SUMMARY"

      - name: Run Tests
        run: cargo test --verbose --all-features --locked

      - name: Job summary
        run: echo "- ✅ cargo test (5/7)" >> "$GITHUB_STEP_SUMMARY"

      - name: Install cross (for arm64 builds)
        uses: taiki-e/install-action@v2
        with:
          tool: cross

      - name: Build release binary (linux/amd64)
        run: |
          cargo build --release --locked
          test -f target/release/rust_oauth2_server
          cp target/release/rust_oauth2_server target/release/rust_oauth2_server-amd64

      - name: Build release binary (linux/arm64)
        run: |
          cross build --release --locked --target aarch64-unknown-linux-gnu
          test -f target/aarch64-unknown-linux-gnu/release/rust_oauth2_server
          mkdir -p target/release
          cp target/aarch64-unknown-linux-gnu/release/rust_oauth2_server target/release/rust_oauth2_server-arm64

      - name: Job summary
        run: echo "- ✅ cargo build --release (6/7)" >> "$GITHUB_STEP_SUMMARY"

      - name: Upload release binary artifact (linux/amd64)
        uses: actions/upload-artifact@v4
        with:
          name: rust_oauth2_server-linux-amd64
          path: target/release/rust_oauth2_server-amd64
          if-no-files-found: error

      - name: Upload release binary artifact (linux/arm64)
        uses: actions/upload-artifact@v4
        with:
          name: rust_oauth2_server-linux-arm64
          path: target/release/rust_oauth2_server-arm64
          if-no-files-found: error

      - name: Job summary (final)
        if: always()
        run: |
          {
            echo "- ✅ Upload artifact (7/7)"
            echo
            echo "Result: **${{ job.status }}**"
          } >> "$GITHUB_STEP_SUMMARY"

  # Security Scanning
  security:
    name: Security (audit, deny)
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Job summary (init)
        run: |
          {
            echo "## Security (audit, deny)"
            echo "- ✅ Checkout (1/4)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Rust toolchain + cache
        uses: ./.github/actions/rust-setup
        with:
          toolchain: stable
          cache-key: security
          install-system-deps: "true"

      - name: Job summary
        run: echo "- ✅ Rust toolchain + cache (2/4)" >> "$GITHUB_STEP_SUMMARY"

      - name: Install cargo-audit
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit

      - name: Job summary
        run: echo "- ✅ cargo-audit installed (3/4)" >> "$GITHUB_STEP_SUMMARY"

      - name: Security Audit (cargo-audit)
        continue-on-error: true
        run: cargo audit

      - name: Check Dependencies (cargo-deny)
        continue-on-error: true
        uses: EmbarkStudios/cargo-deny-action@v1
        with:
          command: check

      - name: Job summary (final)
        if: always()
        run: |
          {
            echo "- ✅ cargo audit + cargo deny completed (4/4)"
            echo
            echo "Result: **${{ job.status }}** (continue-on-error: true)"
          } >> "$GITHUB_STEP_SUMMARY"

  # Code Coverage
  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    needs: [ci]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Job summary (init)
        run: |
          {
            echo "## Code Coverage"
            echo "- ✅ Checkout (1/4)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Rust toolchain + cache
        uses: ./.github/actions/rust-setup
        with:
          toolchain: stable
          cache-key: coverage
          install-system-deps: "true"

      - name: Job summary
        run: echo "- ✅ Rust toolchain + cache (2/4)" >> "$GITHUB_STEP_SUMMARY"

      - name: Install tarpaulin
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-tarpaulin

      - name: Job summary
        run: echo "- ✅ cargo-tarpaulin installed (3/4)" >> "$GITHUB_STEP_SUMMARY"

      - name: Generate Coverage
        run: cargo tarpaulin --out Xml --output-dir ./coverage --all-features --locked

      - name: Upload Coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: ./coverage/cobertura.xml
          fail_ci_if_error: false

      - name: Job summary (final)
        if: always()
        run: |
          {
            echo "- ✅ Coverage uploaded (4/4)"
            echo
            echo "Result: **${{ job.status }}**"
          } >> "$GITHUB_STEP_SUMMARY"

  # Integration Tests
  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: [ci]
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: oauth2_test
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - name: Job summary (init)
        run: |
          {
            echo "## Integration Tests"
            echo "- ✅ Checkout (1/4)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Rust toolchain + cache
        uses: ./.github/actions/rust-setup
        with:
          toolchain: stable
          cache-key: integration
          install-system-deps: "true"

      - name: Job summary
        run: echo "- ✅ Rust toolchain + cache (2/4)" >> "$GITHUB_STEP_SUMMARY"

      - name: Run Flyway Migrations
        run: |
          docker run --rm --network host -v ${{ github.workspace }}/migrations/sql:/flyway/sql \
            flyway/flyway:10-alpine \
            -url=jdbc:postgresql://localhost:5432/oauth2_test \
            -user=test_user \
            -password=test_password \
            -connectRetries=20 \
            -connectRetriesInterval=3 \
            -locations=filesystem:/flyway/sql \
            migrate

      - name: Job summary
        run: echo "- ✅ Flyway migrations applied (3/4)" >> "$GITHUB_STEP_SUMMARY"

      - name: Run Integration Tests
        env:
          OAUTH2_DATABASE_URL: postgresql://test_user:test_password@localhost:5432/oauth2_test
        run: cargo test --test integration --locked

      - name: Job summary (final)
        if: always()
        run: |
          {
            echo "- ✅ Integration test suite finished (4/4)"
            echo
            echo "Result: **${{ job.status }}**"
          } >> "$GITHUB_STEP_SUMMARY"

  mongo-storage:
    name: Mongo Storage Tests (feature)
    runs-on: ubuntu-latest
    needs: [ci]
    steps:
      - uses: actions/checkout@v4

      - name: Job summary (init)
        run: |
          {
            echo "## Mongo Storage Tests (feature)"
            echo "- ✅ Checkout (1/3)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Rust toolchain + cache
        uses: ./.github/actions/rust-setup
        with:
          toolchain: stable
          cache-key: mongo-storage
          install-system-deps: "true"

      - name: Job summary
        run: echo "- ✅ Rust toolchain + cache (2/3)" >> "$GITHUB_STEP_SUMMARY"

      - name: Run Mongo storage tests (requires Docker)
        env:
          RUN_TESTCONTAINERS: "1"
        run: cargo test --test mongo_storage --features mongo --locked

      - name: Job summary (final)
        if: always()
        run: |
          {
            echo "- ✅ Mongo storage tests finished (3/3)"
            echo
            echo "Result: **${{ job.status }}**"
          } >> "$GITHUB_STEP_SUMMARY"

  # E2E Tests on KIND (reuses prebuilt binary from the 'ci' job)
  e2e-kind:
    name: E2E Tests on KIND
    runs-on: ubuntu-latest
    needs: [ci]
    timeout-minutes: 35
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Job summary (init)
        run: |
          {
            echo "## E2E Tests on KIND"
            echo "- ✅ Checkout (1/8)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Download prebuilt server binaries
        uses: actions/download-artifact@v4
        with:
          name: rust_oauth2_server-linux-amd64
          path: target/release

      - name: Download prebuilt server binary (arm64)
        uses: actions/download-artifact@v4
        with:
          name: rust_oauth2_server-linux-arm64
          path: target/release

      - name: Job summary
        run: echo "- ✅ Download binary (2/8)" >> "$GITHUB_STEP_SUMMARY"

      - name: Ensure binaries are executable
        run: chmod +x target/release/rust_oauth2_server-amd64 target/release/rust_oauth2_server-arm64

      - name: Job summary
        run: echo "- ✅ Binary prepared (3/8)" >> "$GITHUB_STEP_SUMMARY"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Job summary
        run: echo "- ✅ Docker Buildx ready (4/8)" >> "$GITHUB_STEP_SUMMARY"

      - name: Install KIND
        shell: bash
        run: |
          set -euo pipefail
          KIND_VERSION="v0.24.0"
          curl -fsSL -o kind "https://kind.sigs.k8s.io/dl/${KIND_VERSION}/kind-linux-amd64"
          chmod +x kind
          sudo mv kind /usr/local/bin/kind
          kind version

      - name: Job summary
        run: echo "- ✅ KIND installed (5/8)" >> "$GITHUB_STEP_SUMMARY"

      - name: Install kustomize
        run: |
          curl -s "https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh" | bash
          sudo mv kustomize /usr/local/bin/

      - name: Job summary
        run: echo "- ✅ kustomize installed (6/8)" >> "$GITHUB_STEP_SUMMARY"

      - name: Enable config debug logging
        run: echo "OAUTH2_DEBUG_CONFIG=1" >> $GITHUB_ENV

      - name: Build Docker image (from prebuilt binary)
        timeout-minutes: 20
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.prebuilt
          tags: docker.io/ianlintner068/oauth2-server:test
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Job summary
        run: echo "- ✅ Docker image built (7/8)" >> "$GITHUB_STEP_SUMMARY"

      - name: Run E2E (shared script)
        shell: bash
        run: |
          set -euo pipefail
          # Keep cluster + namespace so the next steps can export/upload logs if the script fails.
          SKIP_IMAGE_BUILD=1 \
          KEEP_CLUSTER=1 \
          KEEP_NAMESPACE=1 \
          CLUSTER_NAME=oauth2-test \
          NAMESPACE=oauth2-server \
          IMAGE_REF=docker.io/ianlintner068/oauth2-server:test \
          bash scripts/e2e_kind.sh

      - name: Job summary (final)
        if: always()
        run: |
          {
            echo "- ✅ E2E run finished (8/8)"
            echo
            echo "Result: **${{ job.status }}**"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Export cluster logs
        if: failure()
        run: |
          kubectl logs deployment/oauth2-server -n oauth2-server --tail=200 > oauth2-server.log || echo "No oauth2-server logs"
          kubectl describe pods -n oauth2-server > pods-describe.log

      - name: Upload logs
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: kind-cluster-logs
          path: |
            oauth2-server.log
            pods-describe.log
          retention-days: 7

      - name: Cleanup
        if: always()
        run: |
          kubectl delete namespace oauth2-server || true
          kind delete cluster --name oauth2-test || true

  # Docker Build
  docker:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: [ci, security, integration, mongo-storage, e2e-kind]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/'))
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Job summary (init)
        run: |
          {
            echo "## Docker Build & Push"
            echo "- ✅ Checkout (1/6)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Download prebuilt server binaries
        uses: actions/download-artifact@v4
        with:
          name: rust_oauth2_server-linux-amd64
          path: target/release

      - name: Download prebuilt server binary (arm64)
        uses: actions/download-artifact@v4
        with:
          name: rust_oauth2_server-linux-arm64
          path: target/release

      - name: Job summary
        run: echo "- ✅ Download binary (2/6)" >> "$GITHUB_STEP_SUMMARY"

      - name: Ensure binaries are executable
        run: chmod +x target/release/rust_oauth2_server-amd64 target/release/rust_oauth2_server-arm64

      - name: Job summary
        run: echo "- ✅ Binary prepared (3/6)" >> "$GITHUB_STEP_SUMMARY"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Job summary
        run: echo "- ✅ Buildx + QEMU ready (4/6)" >> "$GITHUB_STEP_SUMMARY"

      - name: Decide publishing targets
        id: publish
        shell: bash
        run: |
          set -euo pipefail
          if [[ -n "${{ secrets.DOCKER_USERNAME }}" && -n "${{ secrets.DOCKER_TOKEN }}" ]]; then
            echo "dockerhub=true" >> "$GITHUB_OUTPUT"
          else
            echo "dockerhub=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Login to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Login to Docker Hub
        if: steps.publish.outputs.dockerhub == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Compute release version (tags only)
        if: startsWith(github.ref, 'refs/tags/')
        shell: bash
        run: |
          set -euo pipefail
          ref_name='${{ github.ref_name }}'
          version="${ref_name#v}"

          # Enforce numeric semver-ish tags for releases (e.g., 0.0.1 or v0.0.1, optionally with -rc.1).
          if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+([.-].+)?$ ]]; then
            echo "Tag '$ref_name' is not a valid release version (expected like 0.0.1 or v0.0.1)."
            exit 1
          fi

          echo "IMAGE_VERSION=$version" >> "$GITHUB_ENV"

      - name: Extract metadata (GHCR)
        id: meta_ghcr
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository_owner }}/oauth2-server
          tags: |
            # Main branch publishes: latest + commit SHA
            type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
            type=sha,format=short,enable=${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}

            # Tag publishes: versioned image (e.g., 0.0.1)
            type=raw,value=${{ env.IMAGE_VERSION }},enable=${{ startsWith(github.ref, 'refs/tags/') }}

      - name: Extract metadata (Docker Hub)
        if: steps.publish.outputs.dockerhub == 'true'
        id: meta_dockerhub
        uses: docker/metadata-action@v5
        with:
          images: |
            docker.io/${{ secrets.DOCKER_USERNAME }}/oauth2-server
          tags: |
            # Main branch publishes: latest + commit SHA
            type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
            type=sha,format=short,enable=${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}

            # Tag publishes: versioned image (e.g., 0.0.1)
            type=raw,value=${{ env.IMAGE_VERSION }},enable=${{ startsWith(github.ref, 'refs/tags/') }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          file: Dockerfile.prebuilt
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ steps.meta_ghcr.outputs.tags }}
            ${{ steps.meta_dockerhub.outputs.tags }}
          labels: ${{ steps.meta_ghcr.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Validate multi-arch manifest (GHCR)
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${{ startsWith(github.ref, 'refs/tags/') }}" == "true" ]]; then
            tag="${{ env.IMAGE_VERSION }}"
          else
            tag="latest"
          fi

          image="ghcr.io/${{ github.repository_owner }}/oauth2-server:${tag}"
          echo "Inspecting manifest for: $image"

          docker buildx imagetools inspect "$image" | tee /tmp/imagetools.txt
          grep -Eqi 'linux/amd64' /tmp/imagetools.txt
          grep -Eqi 'linux/arm64' /tmp/imagetools.txt

      - name: Job summary (final)
        if: always()
        run: |
          {
            echo "- ✅ Image build/push complete (5/6)"
            echo "- ℹ️  Version: `${{ env.IMAGE_VERSION }}`"
            echo
            echo "Result: **${{ job.status }}**"
          } >> "$GITHUB_STEP_SUMMARY"

  # Documentation
  docs:
    name: Build and Deploy Documentation
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Job summary (init)
        run: |
          {
            echo "## Documentation (MkDocs + Rustdoc)"
            echo "- ✅ Checkout (1/6)"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Rust toolchain + cache
        uses: ./.github/actions/rust-setup
        with:
          toolchain: stable
          cache-key: docs
          install-system-deps: "true"

      - name: Job summary
        run: echo "- ✅ Rust toolchain + cache (2/6)" >> "$GITHUB_STEP_SUMMARY"

      - name: Build Rust Docs
        run: cargo doc --no-deps --all-features

      - name: Job summary
        run: echo "- ✅ Rustdoc built (3/6)" >> "$GITHUB_STEP_SUMMARY"

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements-docs.txt', 'mkdocs.yml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install MkDocs
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements-docs.txt ]; then
            pip install -r requirements-docs.txt
          else
            # Fallback: minimal deps for mkdocs.yml (keep in sync with requirements-docs.txt)
            pip install mkdocs-material mkdocs-mermaid2-plugin
          fi

      - name: Job summary
        run: echo "- ✅ MkDocs deps installed (4/6)" >> "$GITHUB_STEP_SUMMARY"

      - name: Build MkDocs
        run: |
          if [ -f mkdocs.yml ]; then
            mkdocs build
          fi

      - name: Job summary
        run: echo "- ✅ MkDocs site built (5/6)" >> "$GITHUB_STEP_SUMMARY"

      - name: Prepare docs for deployment
        run: |
          mkdir -p _site
          # Copy MkDocs site if it exists
          if [ -d "site" ]; then
            cp -r site/* _site/
          fi
          # Copy Rust docs to rustdoc subdirectory
          if [ -d "target/doc" ]; then
            mkdir -p _site/rustdoc
            cp -r target/doc/* _site/rustdoc/
          fi

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Job summary (final)
        if: always()
        run: |
          {
            echo "- ✅ Pages deployment step finished (6/6)"
            echo
            echo "Result: **${{ job.status }}**"
          } >> "$GITHUB_STEP_SUMMARY"

  # Quality Gate
  gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    needs: [ci, security, integration, mongo-storage, e2e-kind, coverage]
    if: always()
    steps:
      - name: Job summary (init)
        run: |
          {
            echo "## Quality Gate"
            echo "This job evaluates results from other jobs."
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Check Job Results
        run: |
          # Security scan results are informational only
          if [ "${{ needs.security.result }}" != "success" ]; then
            echo "⚠️  Security scan found issues (informational only)"
          fi

          # Coverage is informational and may be skipped
          if [ "${{ needs.coverage.result }}" = "failure" ]; then
            echo "⚠️  Coverage job failed (informational only)"
          fi

          # Only fail on critical job failures
          if [ "${{ needs.ci.result }}" != "success" ] || \
             [ "${{ needs.integration.result }}" != "success" ] || \
             [ "${{ needs.mongo-storage.result }}" != "success" ] || \
             [ "${{ needs.e2e-kind.result }}" != "success" ]; then
            echo "Quality gate failed!"
            exit 1
          fi
          echo "Quality gate passed!"

      - name: Job summary (final)
        if: always()
        run: |
          {
            echo
            echo "Result: **${{ job.status }}**"
            echo "\nUpstream results:"
            echo "- ci: `${{ needs.ci.result }}`"
            echo "- security: `${{ needs.security.result }}`"
            echo "- integration: `${{ needs.integration.result }}`"
            echo "- mongo-storage: `${{ needs.mongo-storage.result }}`"
            echo "- e2e-kind: `${{ needs.e2e-kind.result }}`"
            echo "- coverage: `${{ needs.coverage.result }}`"
          } >> "$GITHUB_STEP_SUMMARY"
