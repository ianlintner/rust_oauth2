apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Extended E2E overlay for KIND/local testing.
# Adds additional components (resource server) on top of the existing e2e-kind overlay.

resources:
  - ../e2e-kind
  - ../../components/resource-server

images:
  - name: docker.io/ianlintner068/oauth2-resource-server
    newTag: test
